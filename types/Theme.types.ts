export type Theme = {
    theme: {
        dark: {
            background: string
            text: string
            primary: string
            secondary: string
        }
        light: {
            background: string
            text: string
            primary: string
            secondary: string
        }
    }
}
